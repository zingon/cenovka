<?php $__env->startSection('content'); ?>
<?php echo Form::open(array('route'=>'select.store')); ?>

<table class="table table-striped table-bordered" id="mainTable">
        <thead>
        <tr>
        	<th></th>
        	<th>Kód</th>
            <th>Název</th>
            <th>Cena</th>
            <th>Poznámka</th>
            <th>Počet</th>
            <th>Sleva<th>
        </tr>
        </thead>
        <tbody>
        
            <?php foreach($items as $item): ?>
        <tr>
        	<td><?php echo Form::checkbox('selected['.$item->id.']',$item->id,false); ?></td>
            <td><?php echo $item->code; ?></td>
            <td><?php echo $item->name; ?></td>
            <td><?php echo $item->price; ?></td>
            <td><?php echo $item->note; ?></td>
            <td><?php echo Form::number('count['.$item->id.']'); ?></td>
            <td><?php echo Form::number('discount['.$item->id.']'); ?></td>
        </tr>
            <?php endforeach; ?>
        <?php echo Form::submit('Přidat položky'); ?>

        </tbody>
 
    </table>
   
<?php echo Form::close(); ?>

<?php $__env->stopSection(); ?>
<?php echo $__env->make('layout.main', array_except(get_defined_vars(), array('__data', '__path')))->render(); ?>